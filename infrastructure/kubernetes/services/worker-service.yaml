apiVersion: v1
kind: Service
metadata:
  name: worker-service
  namespace: djqueue
spec:
  selector:
    app: worker-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP