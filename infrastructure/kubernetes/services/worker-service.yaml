apiVersion: v1
kind: Service
metadata:
  name: worker-service
  namespace: djqueue
spec:
  selector:
    app: worker-service
  ports:
    - port: 80
      targetPort: 8082
  type: ClusterIP