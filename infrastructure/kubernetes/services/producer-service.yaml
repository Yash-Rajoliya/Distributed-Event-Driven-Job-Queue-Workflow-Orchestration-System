apiVersion: v1
kind: Service
metadata:
  name: job-producer
  namespace: djqueue
spec:
  selector:
    app: job-producer
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP